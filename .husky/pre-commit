# Format code
echo "Running Prettier..."
# pnpm format

# Lint source code
echo "Running ESLint..."
# pnpm lint

# Abort commit if tests or lint fail
if [ $? -ne 0 ]; then
  echo "Lint failed. Aborting commit..."
  exit 1
fi